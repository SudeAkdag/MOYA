import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Tasarımdaki Ana Yeşil (Doğa Terapisi Teması için)
  static const Color primaryGreen = Color(0xFF2D6A4F); 
  static const Color secondaryGreen = Color(0xFF40916C);
  static const Color accentGreen = Color(0xFFD8F3DC);

  // Koyu Mod Arka Planları
  static const Color darkBackground = Color(0xFF121212); // Simsiyah değil, göz yormayan koyu gri
  static const Color surfaceColor = Color(0xFF1E1E1E);   // Kartların rengi

  // Metin Renkleri
  static const Color textWhite = Color(0xFFEEEEEE);
  static const Color textGrey = Color(0xFFB0B0B0);
  
  // Mood Tracker Renkleri (Gülücükler için)
  static const Color moodBad = Color(0xFFEF5350);
  static const Color moodNeutral = Color(0xFFFFCA28);
  static const Color moodGood = Color(0xFF66BB6A);
}